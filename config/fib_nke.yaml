# NKE fibonacci retrace preset (non-tech stock, account 2).
# strategy: fibonacci — won the 6-month backtest on this symbol.
# Inherits all other defaults from src/gold_trader/config.py.
symbol: NKE
timeframe: H1
strategy: fibonacci

risk:
  per_trade_pct: 0.3
  atr_stop_mult: 2.0
  max_positions: 1

filters:
  atr_pct_min: 0.001
  atr_pct_max: 0.10
  adx_min: 25.0

daily_guard:
  max_consecutive_losses: 2
  max_loss_pct: 2.0

session:
  trade_days: [Mon, Tue, Wed, Thu, Fri]

execution:
  magic_number: 20260750
  deviation_points: 30
  comment: "fib_nke"
